name: Airlock

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  airlock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: xdlc-labs/airlock@v0.1.0-beta.7
