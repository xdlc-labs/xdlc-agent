apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "xdlc-agent.fullname" . }}-config
  labels:
    {{- include "xdlc-agent.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Values.config | indent 4 }}
