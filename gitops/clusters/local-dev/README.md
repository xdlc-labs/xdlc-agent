# local-dev

Placeholder for a per-cluster app tree. Today apps still live in
`gitops/apps/dev/` and the ApplicationSet (or `root-dev.yaml`) points there.

Move manifests here only when a cluster needs a different app set than
shared `gitops/apps/` — until then, leave this empty.
